{
  "name": "medicare-ai",
  "version": "1.0.0",
  "description": "MEDICARE AI - Medical Assistant with Gemini AI",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "npx prisma generate --schema=backend/prisma/schema.prisma",
    "vercel-build": "npx prisma generate --schema=backend/prisma/schema.prisma",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "@prisma/client": "^6.19.2",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
